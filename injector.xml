<?xml version='1.0' encoding='utf-8'?>
<html xmlns="http://www.w3.org/1999/xhtml" xml:lang="en" lang="en">

<h2>The Zero Install Injector</h2>

<program name='Zero Install Injector'
	 author='Thomas Leonard'
	 git='http://repo.or.cz/w/zeroinstall.git'
	 license='GNU Lesser General Public License'/>

<p>
The injector (<b>0launch</b>) is a small Python program. You ask it to run a program by giving
it the program's full name (a web address). The injector does <b>not</b> need
to be installed or run as root. It does <b>not</b> write to any shared
directories (like <b>/usr/bin</b> or <b>/usr/lib</b>). It stores downloads in a cache directory
so that you don't need to download anything twice, and programs can still be used when you are off-line.
</p>

<table class='quicklinks'>
 <tr>
  <td><a href='#packages'><img src='tango/go-down.png' alt='' width='48' height='48'/> <br/>Install it!</a></td>
  <td><a href='doc.html'><img src='tango/book.png' alt='' width='48' height='48'/> <br/>Read more</a></td>
  <td><a href='dev.html'><img src='tango/applications-development.png' alt='' width='48' height='48'/> <br/>Developers</a></td>
  <td><a href='faq.html'><img src='tango/help-browser.png' alt='' width='48' height='48'/> <br/>FAQ</a></td>
 </tr>
</table>

<ul>
 <li><a href='http://www.gnomefiles.org/version.php?soft_id=921'>Release notes</a> (all versions)</li>
</ul>

<h2><a name='packages'/>Packages</h2>

<dl>
  <dt>RPMs</dt>
<dd>
<ul>
  <li><b>Fedora</b> - a <a href='http://koji.fedoraproject.org/koji/packageinfo?packageID=4282'>package</a> is in Fedora Extras (thanks to Michel Alexandre Salim), so just do:
  <pre># <b>yum install zeroinstall-injector</b></pre>
  <p><strong>Fedora 7: </strong>Fedora 7 went out with an old version which couldn't install anything (see <a href='https://bugzilla.redhat.com/bugzilla/show_bug.cgi?id=250168'>bug #250168</a>). This has now been fixed in an update.</p>
  </li>
  <li><b>SUSE</b> <download name='zeroinstall-injector-0.27-1.suse_noarch.rpm'/> (SUSE 9.3 RPM) (contributed by Thomas Formella) [ <download name='zeroinstall-injector-0.19-1suse.spec'/> (.spec file) ]</li>
  <li><b>OpenSUSE</b> <download name='zeroinstall-injector-0.31-4.1.i386.rpm'/> (OpenSUSE 10.3 RPM) (contributed by Thomas Formella)</li>
</ul>
<p><b>Experimental binary and source RPMs: </b> More up-to-date RPMs are available for some
systems through the <a href='http://software.opensuse.org/search?q=zeroinstall-injector&amp;p=1&amp;baseproject=ALL'>SUSE build service</a>.
Please test these and report back (<i>DO NOT</i> install the -test packages). Note
that the Mandriva package doesn't work on 2007.1 without a
<a href='http://article.gmane.org/gmane.comp.file-systems.zero-install.devel/1792'>minor fix</a>.</p>
</dd>

<dt>Debs</dt>
<dd>
<ul>
  <li><b>Debian/oldstable (sarge)</b> <download name='zeroinstall-injector_0.22-1_all.deb'/> (Debian package; Python 2.3)<br/>
  <b>Debian/stable (etch)</b> <download name='zeroinstall-injector_0.31-1_all.deb'/> (Debian package; python-central)<br/>
  <b>Debian/testing (lenny)</b> Zero Install is part of the main Debian archive, so you can get it with:
<pre># <b>apt-get install zeroinstall-injector</b></pre></li>
  <li><b>Ubuntu (breezy/dapper)</b> <download name='zeroinstall-injector_0.22-1_all_ubuntu.deb'/> (Ubuntu package; Python 2.4)<br/>
  <b>Ubuntu (edgy)</b> <download name='zeroinstall-injector_0.30-0ubuntu1_all.deb'/><br/>
  <b>Ubuntu (feisty/gutsy)</b> Zero Install is in Feisty and Gutsy (<a href='https://help.ubuntu.com/6.10/ubuntu/desktopguide/C/extra-repositories.html'>Universe</a>), so you can just do:
<pre>$ <b>sudo apt-get install zeroinstall-injector</b></pre>
  <p>However, the edgy deb is more up-to-date than the version in feisty, so you may prefer to install that.</p>
  </li>
  <li><b>Knoppix</b> 5 works using the Debian package; use <b>apt-get update; apt-get install -f</b> to fetch the dependencies</li>
  <li><b>Nokia 770</b> <a href='http://web.telia.com/~u86012345/nokia770/'>zeroinstall-injector_0.19_arm.deb download page</a> (Nokia 770 Debian package contributed by Niklas Höglund; Python 2.4)</li>
  <li><a href='http://article.gmane.org/gmane.comp.desktop.rox.user/6759'>Creating a .deb from source</a></li>
</ul>
</dd>

<dt>Other</dt>
<dd>
<ul>
  <li><b>Slackware 10.2</b> <download name="0install-0.27-noarch-1_slack10.2.tgz"/> (Slackware 10.2 package) (contributed by Peter Santoro)<br/>
      <b>Slackware 12</b> <download name="zeroinstall-injector-noarch-0.30-1_slack12.tgz"/> (Slackware 12 package) (contributed by MilkaJinka)</li>
  <li><b>FreeBSD</b> <a href='http://www.freshports.org/devel/zeroinstall-injector/'>/usr/ports/devel/zeroinstall-injector</a> (Dylan Cochran)</li>
  <li><b>Gentoo</b> <a href='http://gentoo-portage.com/rox-base/zeroinstall-injector'>zeroinstall-injector</a> is in Portage.</li>
  <li><b>Solaris</b> works (follow instructions below)</li>
  <li><a href="http://thread.gmane.org/gmane.comp.desktop.rox.devel/7405">GoboLinux instructions</a> (anyone want to create a recipe?)</li>
  <li><b>Mac OS X</b> <a href='http://trac.macosforge.org/projects/macports/browser/trunk/dports/sysutils/zeroinstall-injector/'>sysutils/zeroinstall-injector</a> in <a href='http://www.macports.org/'>MacPorts</a> (Anders F Björklund):<br/>
  <pre>$ sudo port install zeroinstall-injector</pre></li>
  <li><b>Microsoft Windows</b> is not currently supported, as it lacks the <i>fork</i> system call. This should be easy to work around, if any Windows developers want to volunteer.</li>
  <li>Other (follow instructions below)</li>
</ul>
</dd>
</dl>

<p>
If an injector package for your distribution is listed above, install that and then <a href='injector-using.html'>try using it</a>!
Otherwise, follow the instructions below to install using the generic tarball.
</p>

<h2>Installing from tarball</h2>

<p>
To use the injector, you will need:
</p>

<ul>
<li><a href="http://python.org">Python</a> (version 2.4 or later, <i>including</i> any <b>-dev</b> package).</li>
<li><a href="http://www.gnupg.org/">GnuPG</a> (to check the digital signatures).</li>
<li><a href="http://freshmeat.net/projects/pygtk/?branch_id=12503">PyGTK 2.0 or later, including python-glade2</a> (only needed for the GUI).</li>
</ul>

<p>
Download the latest .tar.bz2 archive from <a
href="http://sourceforge.net/project/showfiles.php?group_id=76468&amp;package_id=146899">the
download page</a> (and the corresponding .sig file if you want to verify it), or using wget:
</p>
<pre>
$ <b>wget http://osdn.dl.sourceforge.net/sourceforge/zero-install/zeroinstall-injector-0.31.tar.bz2</b>
$ <b>wget http://osdn.dl.sourceforge.net/sourceforge/zero-install/zeroinstall-injector-0.31.tar.bz2.sig</b>
</pre>

<p>
Check that the signature is correct using GPG (this also ensures that GPG is installed correctly).
You'll need <a href="http://0install.net/public_key.gpg">my GPG key</a>, which you can download and import
using --recv-key (or <a href='http://rox.sourceforge.net/desktop/GPG'>manually</a>):
</p>

<pre>
$ <b>gpg --recv-key --keyserver www.keyserver.net 59A53CC1</b>
$ <b>gpg zeroinstall-injector-0.31.tar.bz2.sig</b>
gpg: Signature made Sun Nov 18 18:33:06 2007 UTC using DSA key ID 59A53CC1
gpg: Good signature from "Thomas Leonard &lt;XXXXX@gmail.com>"
gpg: Good signature from "Thomas Leonard &lt;XXXXX@users.sourceforge.net&gt;"
gpg: WARNING: This key is not certified with a trusted signature!
gpg:          There is no indication that the signature belongs to the owner.
Primary key fingerprint: 9242 9807 C985 3C07 44A6  8B9A AE07 8280 59A5 3CC1
</pre>

<p>
The warning is just to tell you that you haven't yet established that the key really
belongs to me (ultra-paranoid users will now cross-check the finger-print using
some other source of information).
</p>

<p>
You should now extract the archive, and change directory inside it:
</p>
<pre>
$ <b>tar xjf zeroinstall-injector-0.31.tar.bz2</b>
$ <b>cd zeroinstall-injector-0.31</b>
</pre>

<table class='split'>
 <col id="col1"/>
 <tr><th>Installing normally</th><th>Installing without root access</th></tr>
 <tr>
  <td>
  <p>For a normal install, su to root, install, and exit from root:</p>
<pre>$ <b>su</b>
[ enter root password ]
# <b>python setup.py install</b>
# <b>exit</b>
</pre>
 </td><td>
<p>
If you don't have the root password, use this command.
You'll need to make sure that ~/bin is in $PATH, and
~/lib/python is in $PYTHONPATH in this case.
</p>
<pre>$ <b>python setup.py install --home ~</b>
$ <b>export PATH=$HOME/bin:$PATH</b>
$ <b>export PYTHONPATH=$HOME/lib/python</b>
</pre>
  </td>
 </tr>
</table>

<dl>
<dt>error: invalid Python installation: unable to open /usr/lib/python2.3/config/Makefile</dt>
<dd>You need to install the <i>python-dev</i> package, not just <i>python</i>.</dd>
</dl>

<p>
You now have two new commands: <b>0launch</b>, which takes the URL of a program and runs it, and
<b>0alias</b>, which lets you use short names instead of URLs.
</p>

<p>
Now it's time to <a href='injector-using.html'>try using it...</a>
</p>

</html>
